# Automatic-Work-Hour-Generator-.xlsx-
Will get the hours and date worked, and compile it into an excel workbook. *Note, the name written on the doc has to be modified for every user, by default it's mine.
